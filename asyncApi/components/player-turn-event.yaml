components:
  schemas:
    PlayerTurnEvent:
      type: object
      properties:
        eventCode:
          type: string
          description: Код события.
        data:
          type: object
          description: Объект сопутствующих данных.
          properties:
            playerId:
              type: string
              description: Индификатор игрока.
            cardId:
              type: string
              description: Индификатор карты.
              