components:
  schemas:
    GameEndEvent:
      type: object
      properties:
        eventCode:
          type: string
          description: Код окончания игры.
        data:
          type: object
          description: Объект сопутствующих данных.
          properties:
            $ref: 'games.yaml#/components/schemas/Game/items/properties/winner'
